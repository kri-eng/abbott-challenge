import 'package:flutter/material.dart';
import 'package:challenge/app.dart';

/// The entry point of the Flutter application.
///
/// This file initializes and runs the root widget of the app.
/// It calls [runApp] with the [MyApp] widget defined in `app.dart`.
void main() {
  runApp(const MyApp());
}